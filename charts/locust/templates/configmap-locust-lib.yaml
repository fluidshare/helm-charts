{{- if eq .Values.loadtest.configmaps.lib "" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "chart.fullname" . }}-lib
  labels:
    {{- include "chart.labels" . | nindent 4 }}
data:
  {{- ($.Files.Glob (printf "locustfiles/%s/lib/*" .Values.loadtest.name)).AsConfig | nindent 2 }}
{{- end }}
