## To access the locust master UI

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.host }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ . }}
  {{- end }}
{{- end }}
{{- else }}
Run this command:

  kubectl --namespace {{ .Release.Namespace }} port-forward service/{{ include "chart.fullname" . }} 8089:8089

Then open in a browser: http://localhost:8089
{{- end }}
